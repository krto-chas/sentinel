apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sentinel
resources:
  - namespace.yaml
  - configmap.yaml
  # Copy secret.example.yaml to secret.yaml and fill real values before applying.
  - secret.yaml
  - clamav-deployment.yaml
  - clamav-service.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - networkpolicy.yaml

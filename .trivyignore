# =============================================================================
# .trivyignore – Accepterade CVE:er utan tillgänglig fix i Debian 12
# =============================================================================
# Dessa CVE:er gäller OS-nivåpaket i basimagen (python:3.12-slim-bookworm).
# Ingen av dem är fixad i Debian 12 (bookworm) per 2026-02-27.
# Ingen av dem är nåbar via applikationens kodvägar.
# Inför varje ny deploy: verifiera status på https://security-tracker.debian.org
# =============================================================================

# CVE-2026-0861 – glibc: heltalsspill i memalign → heap-korruption (libc-bin, libc6)
# Status: affected, ingen fix i Debian 12. Applikationen anropar inte memalign direkt.
CVE-2026-0861

# CVE-2023-2953 – OpenLDAP: null pointer dereference i ber_memalloc_x (libldap-2.5-0)
# Status: affected, ingen fix i Debian 12. Applikationen använder inte LDAP.
CVE-2023-2953

# CVE-2025-7458 – SQLite: heltalsspill (libsqlite3-0)
# Status: affected, ingen fix i Debian 12. sqlite3 används ej av applikationen direkt.
CVE-2025-7458

# CVE-2023-45853 – zlib: heltalsspill i zipOpenNewFileInZip4_6 (zlib1g)
# Status: will_not_fix (Debian). Berör ZIP-skrivningskod; applikationen skapar inga ZIP-filer.
CVE-2023-45853

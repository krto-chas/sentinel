# Host port used to access MongoDB from your machine (keeps 27017 free if occupied).
MONGO_HOST_PORT=28017

# Host port for Nginx reverse proxy (entrypoint for the app).
NGINX_HOST_PORT=8080

# Root credentials used by MongoDB container (development only).
MONGO_ROOT_USERNAME=sentinel
MONGO_ROOT_PASSWORD=change-me

# Application database name.
MONGO_DB=sentinel_upload

# Optional override for app connection URI.
# Leave commented for local Docker Mongo.
# Uncomment and set this when using MongoDB Atlas/Render.
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# Scanner settings
SCANNER_MODE=auto
CLAMAV_HOST=clamav
CLAMAV_PORT=3310
CLAMAV_TIMEOUT_SECONDS=5

# Auth settings
AUTH_MODE=off

# Upload rate limiting
UPLOAD_RATE_LIMIT_PER_MINUTE=10
UPLOAD_RATE_LIMIT_WINDOW_SECONDS=60
